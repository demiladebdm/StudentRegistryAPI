using BankService.Data;
//using BankService.Models;

namespace StudentService.Data
{
    public class SeedData
    {
        public static void Initialize(ApplicationDbContext context)
        {
            //if (context.Students.Any()) return;

            //context.Students.AddRange(
            //    new Student { PhoneNumber = "1234567890", Email = "john@example.com", Password = "Password123", State = "Lagos", LGA = "Ikeja", IsPhoneVerified = true },
            //    new Student { PhoneNumber = "0987654321", Email = "jane@example.com", Password = "Password123", State = "Kano", LGA = "Gwale", IsPhoneVerified = true }
            //);

            //context.SaveChanges();
        }
    }
}

